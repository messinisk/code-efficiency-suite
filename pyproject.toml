[project]
name = "code-efficiency-suite"
version = "0.0.0"
description = "Code efficiency suite"
readme = "README.md"
requires-python = "<=3.14"
dependencies = [
    "numpy",
    "psutil",
    "decorator"
]
