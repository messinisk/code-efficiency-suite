[build-system]
requires = ["setuptools>=77.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "code-efficiency-suite"
version = "0.0.1"
description = "Code efficiency suite"
authors = [
    { name = "messinis kostas", email = "messiniskostas0@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.14"

dependencies = [
    "numpy",
    "psutil",
    "decorator",
    "Pympler"
]

